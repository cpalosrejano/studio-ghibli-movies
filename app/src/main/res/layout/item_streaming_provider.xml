<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="wrap_content"
    android:layout_width="wrap_content"
    android:padding="8dp">

    <ImageView
        android:id="@+id/img_streaming_provider_image"
        android:layout_width="64dp"
        android:layout_height="64dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:scaleType="centerCrop"
        tools:src="@drawable/img_title"
        android:contentDescription="@string/app_name" />

    <TextView
        android:id="@+id/img_streaming_provider_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        style="@style/BodySmallTextStyle"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/img_streaming_provider_image"
        app:layout_constraintStart_toStartOf="@id/img_streaming_provider_image"
        app:layout_constraintEnd_toEndOf="@id/img_streaming_provider_image"
        tools:text="Netflix" />



</androidx.constraintlayout.widget.ConstraintLayout>